<%= form_for(@newPost) do |f| %>

  <center>
    <div class="field">
      <%= f.text_field :content, autofocus: true %>
    </div>

    <div class="actions">
      <%= f.submit "Sign up" %>
    </div>
  </center>
  
<% end %>

<%= render "devise/shared/links" %>
